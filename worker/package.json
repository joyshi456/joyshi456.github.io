{
  "name": "enjoyshi-events-worker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute enjoyshi-events --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute enjoyshi-events --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "wrangler": "^4.20.0"
  }
}
